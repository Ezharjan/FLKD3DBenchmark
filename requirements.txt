# Dependencies for PointNet/PointNet++ baselines and FL/KD workflows
# Note: PyTorch should be installed separately with CUDA support (see README.md)
# pip install --pre torch torchvision torchaudio --index-url https://download.pytorch.org/whl/nightly/cu128
numpy
matplotlib
pandas
tqdm
sympy
Pillow
scipy
h5py
plyfile        # read .ply datasets (OmniObject3D / YCB / Craniosynostosis)
